# Plugins

This repository contains plugin and extension packages built from a shared source and distributed for multiple CLIs.

## Available Package

- `plugins/nexus-orchestrator`

## Supported Targets

- GitHub Copilot CLI
- Gemini CLI
- Claude Code

## Build Commands

Run from repository root:

```bash
bun run ci
```

Artifacts are generated in `dist/`.
